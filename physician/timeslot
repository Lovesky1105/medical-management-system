<!DOCTYPE html>
<html>
<head>
    <title>Time Slot Booking</title>
    <script>
        function disableTimeSlot() {
            // Get the selected time slot value
            var selectedTimeSlot = document.getElementById("timeslot").value;

            // Disable the selected option
            var option = document.querySelector('option[value="' + selectedTimeSlot + '"]');
            option.disabled = true;

            // Optionally, you can also remove the selected option if you don't want it to be visible
            // option.style.display = "none";
        }
    </script>
</head>
<body>
    <h1>Book a Time Slot</h1>
    <form action="book_time_slot.php" method="post">
        <label for="timeslot">Select a Time Slot:</label>
        <select name="timeslot" id="timeslot" onchange="disableTimeSlot()">
            <option value="slot1">Time Slot 1</option>
            <option value="slot2">Time Slot 2</option>
            <option value="slot3">Time Slot 3</option>
            <!-- Add more time slots as needed -->
        </select>
        <input type="submit" value="Book Time Slot">
    </form>
</body>
</html>